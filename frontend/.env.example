# ── Authentication (Clerk) ──────────────────────────────────────────────
# Publishable key from https://dashboard.clerk.com → API Keys.
# Safe to expose in the browser bundle (this is what Vite ships).
VITE_CLERK_PUBLISHABLE_KEY=pk_test_...
